# 🌌 Obsidian Flow: LuminaLearn — Design Language

## 1. Design Philosophy

* **Focused Immersion** → The UI dissolves away, leaving only content (video, notes, progression).
* **Fluid Interactions** → Every state change feels like water flowing: dissolves, ripples, soft morphs.
* **Encouraging Progression** → Progress is always visible but never loud — glowing bars, ripple checkmarks, subtle milestone celebrations.
* **Playful Sophistication** → Dark, serious foundation with playful glowing accents (blue/violet/green) used sparingly for delight.

---

## 2. Color System (Refined)

### Core Palette

* **Background Deep**: `#05050C` → near-absolute black (canvas).
* **Liquid Glass Base**: `#1A1A2E` @ 70% opacity + blur (main panel material).
* **Lumina Blue** (Primary): `#007AFF` → electric glowing blue (actions, progress).
* **Success Green**: `#34C759` → emerald glow (completion, achievements).
* **Insight Violet**: `#AF52DE` → deep violet (secondary highlights, knowledge points).
* **Text Primary**: `#F0F0F0` (luminous white).
* **Text Secondary**: `#A0A0A0` (soft neutral grey).

### Glow & Shadow Rules

* **Active Glow**: outer glow (`0 0 12px Lumina Blue`) for active buttons/tabs.
* **Success Glow**: soft ripple outward (`0 0 10px Success Green`) on completion.
* **Violet Halo**: subtle inner-shadow with `Insight Violet` for optional/secondary cues.

---

## 3. Typography System

* **Headings**: Rounded geometric sans (Plus Jakarta Sans Rounded / Inter Rounded) — luminous, friendly, but strong.
* **Body**: Inter / Work Sans — clean, neutral.
* **Notes Editor**: Fira Code / JetBrains Mono — monospaced, with subtle violet highlights for markdown.

**Scale (Dark UI optimized)**

* H1 → 40px, glowing edge subtle
* H2 → 32px
* H3 → 24px
* Body → 16px
* Caption → 13px

Use **slight text glow (blue/violet)** for headings in active contexts (like course titles).

---

## 4. Layout System

* **Dashboard/Home** → 2-column grid (main path cards left, quick links + milestones right).
* **Lesson Browser** → Toggle list/gallery view with minimalist pill buttons.
* **Split Screen** → 60/40 split (video/notes), draggable divider with glowing Lumina Blue hover.
* **Panels** → All major UI in frosted liquid glass (`#1A1A2E70` + blur-md).

**Hierarchy**:

* Top nav minimal (icon-only sidebar or floating).
* Dashboard cards = big, glowing pathfinders.
* Lesson rows = sleek, immersive panes.

---

## 5. Components

### 🟦 Primary Action Button

* **Style**: dark glass base + glowing Lumina Blue border.
* **Hover**: faint ripple outward, inner glow intensifies.
* **Active**: ripple animation expands outward.

```html
<button class="relative px-5 py-2.5 rounded-2xl bg-[#1A1A2E]/70 backdrop-blur-md border border-[#007AFF]/60 shadow-[0_0_12px_#007AFF80] hover:shadow-[0_0_20px_#007AFF] transition">
  <span class="text-[#F0F0F0] font-medium">Start Lesson</span>
</button>
```

---

### 📖 Lesson Card (List View)

* **Default**: dark glass row, thumbnail, title, type icon, progress.
* **Hover**: expands slightly, subtle Lumina Blue glow.
* **Progress Bar**: liquid fill animation → Lumina Blue → Success Green when completed.

```html
<div class="group flex items-center gap-4 p-4 rounded-2xl bg-[#1A1A2E]/70 backdrop-blur-md border border-white/5 transition transform hover:scale-[1.02] hover:shadow-[0_0_18px_#007AFF50]">
  <img class="w-20 h-12 rounded-lg object-cover" src="thumb.jpg" />
  <div class="flex-1">
    <h4 class="text-lg font-semibold text-[#F0F0F0]">Lesson Title</h4>
    <div class="h-2 mt-2 rounded-full bg-[#ffffff15]">
      <div class="h-2 rounded-full bg-gradient-to-r from-[#007AFF] to-[#34C759] transition-all duration-700" style="width:45%"></div>
    </div>
  </div>
</div>
```

---

### 🎥 Split-Screen Player + Notes

* **Video**: embedded in dark glass container. Controls appear on hover as glowing, semi-transparent buttons.
* **Notes**: frosted glass editor with monospaced font, subtle ripple glow following cursor.
* **Divider**: thin, glowing Lumina Blue bar that lights up on hover, draggable.

Micro-detail: when typing, a **soft ripple** expands behind the caret.

---

### 🏆 Achievement Badge

* **Appearance**: circular crystal-like glass badge, glowing Success Green checkmark.
* **Animation**: ripple outward + shimmer when unlocked.

---

## 6. Animations & Micro-interactions

* **Ripple Press**: every button tap sends a faint liquid ripple across the surface.
* **Tab Switch**: dissolve/reform transition (opacity + blur + ripple wave).
* **Progress Fill**: smooth “liquid pour” rather than linear bar fill.
* **Milestones**: shimmering wave + ripple burst around the icon.
* **Notes Cursor Glow**: Lumina Blue pulse follows cursor while typing.

**Motion Parameters**

* Ease: `cubic-bezier(.25,.8,.25,1)` (fluid).
* Durations:

  * Micro (hover, taps): 180–220ms
  * Mid (progress, cards): 400–600ms
  * Major (tab/page transitions): 600–800ms

---

## 7. Sidebar Navigation — “Let’s Learn”

* Icons: book (Courses), beaker (Labs), trophy (Achievements).
* **Active Tab**: glowing Lumina Blue halo + animated underline ripple.
* **Inactive Tabs**: soft white-grey with faint hover glow.

Transition: tabs dissolve into liquid glass and reform when switching.

---

## 8. Accessibility & Clarity

* **Contrast**: Lumina Blue (#007AFF) on black (#05050C) has \~8.5:1 ratio → excellent.
* **Success Green & Violet**: use with larger text/icons only; not for small body text.
* **Keyboard**: glowing outline ring (Lumina Blue) on focus.
* **Motion Prefs**: `prefers-reduced-motion` → replace ripples with instant color/opacity changes.

---

## 9. Mood & Atmosphere

* **Background**: pure black with **subtle animated gradients** (Lumina Blue → Violet wave at 5% opacity).
* **Overall Feel**: like studying in a futuristic obsidian-glass workspace, with glowing knowledge nodes lighting up as you progress.
